@model DeveloperDetailsViewModel

<h2>@Model.Name</h2>

<div>
    <p>
        <strong>@Html.DisplayNameFor(model => model.GameRep):</strong> @Model.GameRep
    </p>
    <p>
        <strong>@Html.DisplayNameFor(model => model.LogoLink):</strong> <img src="@Model.LogoLink" alt="Developer Logo" style="max-width: 300px; max-height: 300px;" />
    </p>
</div>

@await Component.InvokeAsync(nameof(GamesShortInfoComponent),Model.Games)